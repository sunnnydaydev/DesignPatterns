名字： Builer模式也叫作建造者模式，或者生成器模式。

特点：1 、是一种对象创建型设计模式
      2 、用来隐藏复合对象的创建过程，他把复合对象的创建过程加以抽象，
          通过子类继承和重载的方式动态的创建具有复合属性的对象。

应用场景：
      1、对象的创建。  builder模式是为了对象的创建而设计的模式
      2、创建的是复合对象。 被创建的对象是一个具有复合属相的对象
      3、关注对象创建的各部分创建过程。不同的工场（builder生成器）对产品属性有不同的创建方法